# 🖥️ 终端基础命令速查表（新手友好版）

> 适合完全不懂终端命令的新手，从这里开始。所有命令都讲清楚：**做什么的、怎么用、例子**。

---

## 📁 目录/文件操作

### 1. `pwd` - 我现在在哪里？
```bash
pwd
```
**作用**：显示当前在哪个目录（文件夹）  
**新手必用**：迷路易迷路了打这个，就知道自己在哪

---

### 2. `cd` - 切换目录
```bash
cd 目录名      # 进到目录里
cd ..          # 回到上一级目录
cd ~           # 回到你的用户目录（就是 /Users/你的名字）
cd -           # 回到刚才那个目录
```
**例子**：
```bash
cd projects    # 进到 projects 文件夹
cd ../downloads # 回到上一级，再进到 downloads
```
**默会技巧**：oh-my-zsh 已经给你加了别名：
```
..  = cd ..
... = cd ../..
.... = cd ../../..
```
所以不用打 `cd ..`，直接打 `..` 回车就行！

---

### 3. `ls` - 列出目录里有什么
```bash
ls             # 简单列出来
ls -l           # 详细列表（权限、大小、修改时间）
ls -la          # 显示隐藏文件
ls -lah         # 详细+显示隐藏+人类可读大小
```

oh-my-zsh 别名：
```
l   = ls -lah    # 👍 推荐！这个最好用
ll  = ls -lh
lsa = ls -lah
```
所以你直接打 `l` 回车就好了，不用记那么多参数！

---

### 4. `mkdir` - 创建新文件夹
```bash
mkdir 文件夹名           # 创建一个文件夹
mkdir -p a/b/c          # 递归创建，父目录不存在也自动创建
```

oh-my-zsh 别名：
```
md = mkdir -p
```
直接 `md 文件夹名` 就完事了

---

### 5. `rm` - 删除文件/文件夹
```bash
rm 文件名          # 删除文件
rm -r 文件夹名      # 删除文件夹（包括里面所有内容）
rm -rf 文件夹名     # 强制删除，不用确认
```

⚠️ **新手注意**：
- `rm -rf /` 会删除整个系统，不要打这个！
- 删除了就找不回来了，确认好了再删

---

### 6. `cp` - 复制
```bash
cp 源文件 目标文件        # 复制文件
cp -r 源文件夹 目标文件夹  # 复制文件夹
```
**例子**：
```bash
cp a.txt b.txt  # 复制 a.txt 成 b.txt
```

---

### 7. `mv` - 移动/重命名
```bash
mv 源 目标        # 移动文件 / 重命名
```
**例子**：
```bash
mv old-name.txt new-name.txt  # 重命名
mv a.txt ~/downloads/         # 移动 a.txt 到 downloads
```

---

### 8. `cat` - 看文件内容
```bash
cat 文件名        # 把整个文件输出到终端
```
适合看短文件，长文件用下面的 `less`

---

### 9. `less` - 分页看长文件
```bash
less 文件名        # 分页显示大文件
```
**操作**：
- 空格 → 下一页
- `b` → 上一页  
- `/关键词` → 搜索  
- `q` → 退出  

**比 cat 好用**，看日志、看代码都用这个

---

### 10. `touch` - 创建空文件
```bash
touch 文件名      # 创建一个空文件
```
比如 `touch README.md`

---

### 11. `find` - 查找文件
```bash
find . -name "*.txt"    # 在当前目录找所有 .txt 文件
find . -name "*.js" | grep -i test  # 找名字含 test 的 js 文件
```

---

## 🔍 查看内容/搜索

### 1. `grep` - 在文件里搜关键词
```bash
grep 关键词 文件名        # 搜文件里哪一行有关键词
grep -r 关键词 目录       # 递归搜整个目录
grep -i 关键词 文件名     # 不区分大小写
```
**例子**：
```bash
grep "error" app.log      # 在 app.log 找 error
grep -r "TODO" src/       # 在 src 目录找所有 TODO 注释
```

---

### 2. `wc` - 统计行数/字数
```bash
wc -l 文件名      # 统计多少行
wc -w 文件名      # 统计多少单词
```
**例子**：
```bash
wc -l *.py       # 统计你的 Python 代码多少行
```

---

### 3. `head` / `tail` - 看文件头尾
```bash
head -n 20 文件  # 看前 20 行
tail -n 20 文件  # 看最后 20 行
tail -f 文件     # 实时看文件新增内容（看日志必备！）
```
**看日志常用**：`tail -f app.log`，日志更新会实时显示，按 `Ctrl+C` 停止

---

## 🏃 运行程序/进程

### 1. `./program` - 运行当前目录下的程序
```bash
./your-script     # 运行可执行文件
```

---

### 2. `&` - 后台运行
```bash
./your-script &   # 程序放后台运行，终端可以继续干别的
```

---

### 3. `Ctrl+C` - 停止当前运行的程序
**你一定会用到**：程序跑错了、卡住了，按 `Ctrl+C` 就停了

---

### 4. `Ctrl+Z` / `fg` / `bg` - 暂停/后台恢复
```bash
Ctrl+Z            # 暂停当前程序，放到后台
bg                # 让后台暂停的程序继续跑
fg                # 把后台程序拉回前台
```

---

### 5. `ps` - 看正在跑的进程
```bash
ps aux            # 看所有进程
ps aux | grep nginx  # 找 nginx 进程
```

---

### 6. `kill` - 杀掉进程
```bash
kill 进程ID        # 杀掉进程
kill -9 进程ID     # 强制杀死，杀不动就用这个
```
**流程**：`ps aux | grep 程序名` → 拿到 PID → `kill PID`

---

## 💁 权限/用户

### 1. `sudo` - 管理员权限运行
```bash
sudo 命令         # 用 root 权限运行命令
```
需要输你的密码（输密码的时候终端不显示星星，正常的）

**oh-my-zsh 技巧**：命令打完才发现要 sudo，不用重新打，直接按 **ESC 两下**，自动加 sudo！

---

### 2. `chmod` - 修改文件权限
```bash
chmod +x 脚本文件    # 给文件加可执行权限（你写了脚本要跑，先chmod +x）
chmod 755 文件夹     # 常见权限设置
```
**权限数字记不住没关系**，要执行就 `chmod +x` 就够了

---

## 💾 压缩/解压

### 1. `tar` - 最常见的压缩格式
```bash
tar -zxvf 文件.tar.gz    # 解压 .tar.gz
tar -Jxvf 文件.tar.xz    # 解压 .tar.xz
tar -czvf 结果.tar.gz 源文件夹  # 压缩成 .tar.gz
```
参数记不住：`z`=gzip, `x`=解压, `c`=压缩, `v`=显示过程, `f`=文件名

---

### 2. `unzip` - 解压 zip
```bash
unzip 文件.zip        # 解压 zip
zip -r 结果.zip 文件夹  # 压缩成 zip
```

---

## 🌐 网络

### 1. `curl` - 请求 HTTP
```bash
curl https://example.com  # 下载网页/接口内容输出
curl -o filename url      # 保存到文件
```

---

### 2. `wget` - 下载文件
```bash
wget https://example.com/file.zip  # 下载文件
```

---

### 3. `ping` - 测试网络通不通
```bash
ping google.com         # ping 一下，看能不能连通
```
按 `Ctrl+C` 停止

---

## 📊 系统信息

### 1. `whoami` - 我是谁
```bash
whoami          # 显示当前用户名
```

---

### 2. `uname -a` - 系统信息
```bash
uname -a        # 显示系统内核、架构等
```

---

### 3. `top` / `htop` - 看系统资源占用
```bash
top             # 看 CPU/内存 占用，哪个进程最吃资源
htop            # 更好看的 top（需要 brew install htop）
```
`q` 退出

---

### 4. `df -h` - 看磁盘还剩多少空间
```bash
df -h           # 人类可读格式显示磁盘使用
```

---

### 5. `du -sh` - 看文件夹多大
```bash
du -sh 目录     # 统计这个目录总共多大
du -sh *        # 统计当前目录下每个文件夹多大
```
**找大文件**：`du -sh * | sort -rh | head -20` 显示前 20 大的文件夹

---

### 6. `free -h` - 看内存使用
```bash
free -h         # 显示总共多少内存，用了多少，剩多少
```

---

## 🔧 环境变量

### 1. `echo $变量名` - 打印环境变量
```bash
echo $PATH      # 看 PATH 环境变量
echo $HOME      # 看你的 home 目录
```

---

### 2. `export` - 设置环境变量
```bash
export PATH="$PATH:/new/path"  # 加路径到 PATH
```
要永久生效写在 `~/.zshrc` 里

---

### 3. `which` - 找命令在哪里
```bash
which python    # 告诉你用的 python 是哪个路径的
which node      # 看看 node 装在哪
```

---

## 📦包管理器（macOS Homebrew）

你是 macOS，用 brew：

```bash
brew search 包名    # 搜包
brew install 包名   # 安装
brew uninstall 包名 # 卸载
brew update         # 更新包列表
brew upgrade        # 升级所有已安装的包
brew list           # 看你装了什么
```

---

## 🎯 新手最常用 15 个命令（按频率排序）

记住这些就够用 80% 场景：

| 命令 | 作用 | 常用别名 |
|------|------|---------|
| `l` | 看当前目录文件 | `ls -lah` |
| `cd ..` → `..` | 往上跳一级 | 直接打 `..` |
| `pwd` | 我在哪 | - |
| `mkdir` → `md` | 新建文件夹 | `md` |
| `touch` | 新建文件 | - |
| `cat` | 看文件 | - |
| `less` | 分页看文件 | - |
| `grep -r` | 搜内容 | - |
| `ps aux` | 看进程 | - |
| `kill` | 杀进程 | - |
| `sudo` | 管理员权限 | 双击 ESC 自动加 |
| `find` | 找文件 | - |
| `tail -f` | 实时看日志 | - |
| `brew install` | 装软件 | - |
| `git` → `g` | 版本控制 | `g` |

---

## 💡 新手必看操作技巧

### 1. 自动补全 - TAB 键
> 打一半命令/文件名，按 **TAB**，自动补全！这个一定要养成习惯，少打很多字还不会错。

### 2. 历史命令 - 方向键上 / Ctrl+R
- 方向键 **↑** → 重复上一条命令
- **Ctrl+R** → 模糊搜历史命令，输入关键词就能找到之前用过的命令（fzf 安装了更爽，可视化选）

### 3. 复制粘贴
- macOS 终端复制：`Cmd+C`，粘贴：`Cmd+V`
- 选中文本就自动复制了（iTerm2 设置里可以开）

### 4. 中断退出
- `Ctrl+C` → 强制退出当前程序
- `Ctrl+D` → 退出当前 shell（相当于 `exit`）

### 5. 清屏
```bash
clear  # 或者 Ctrl+L
```
清屏重新开始，舒服多

---

## 🤔 不知道这个命令干嘛的？`man`/`tldr`

```bash
man 命令名        # 官方完整手册，非常详细
tldr 命令名       # 社区简化例子，需要 brew install tldr，新手推荐！
```
**推荐新手用 `tldr`**，`man` 太全了，看得眼花缭乱，`tldr` 直接给你例子。

---

## ⚡ oh-my-zsh 给你加了什么buff？

| 功能 | 效果 |
|------|------|
| 语法高亮 | 错命令变红，对的变绿，一眼看出错 |
| 自动建议 | 输入开头，它就猜你要输什么，按 → 补全 |
| 目录跳转 `z` | `z 目录名片段` 直接跳，不用一层层 cd |
| 目录书签 `wd` | `wd add name` 打标记，`wd name` 直接跳 |
| Git 别名 | 所有 git 命令都有缩写，少打好多字 |
| Docker 别名 | docker 命令也缩写了 |
| 双击 ESC 加 sudo | 不用重新输命令，太爽了 |

---

## ❓ 常见问题

**Q: 命令输错了怎么快速清空整行？**  
A: `Ctrl+U`，一下子全删了

**Q: 怎么走到行首行尾？**  
A: `Ctrl+A` 行首，`Ctrl+E` 行尾（比按左右箭头快多了）

**Q: 怎么删除光标到行尾？**  
A: `Ctrl+K`

**Q: tab 补全不出来怎么办？**  
A: 你可能拼错了，或者这个东西不存在，检查一下路径

**Q: 我在 `vim` 里出不去怎么办？**  
A: 按 `Esc` 然后输 `:q` 回车，保存是 `:wq`，不保存退出 `:q!`

---

## 📖 进阶一步

当你上面这些都熟悉了，可以开始玩：
- `tmux` - 分屏，一个终端开多个窗口
- `vim`/`neovim` - 终端里直接写代码
- `fzf` - 你已经装了，多按 `Ctrl+R` 搜历史，真香

---

> 记住：终端不可怕，错了就 `Ctrl+C` 重来，多敲几次就熟了。熟能生巧，每天用，一周就熟练了。
