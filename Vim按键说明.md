# vi/vim 按键说明

> **迈克尔·波兰尼的默会知识理论认为：** 人类知识分为两类，**明确知识**（可以用文字、表格清晰表达的按键规则）和**默会知识**（无法完全形式化、需要通过实践掌握的使用技巧）。本说明将同时呈现这两类知识。

---

## 第一部分：一般模式 - 光标移动

### 明确知识：按键对照表

| 按键 | 功能说明 |
|------|----------|
| **h** / ← | 光标向左移动一个字符 |
| **j** / ↓ | 光标向下移动一个字符 |
| **k** / ↑ | 光标向上移动一个字符 |
| **l** / → | 光标向右移动一个字符 |
| `[Ctrl]+[f]` / PageDown | 向下翻一页 (常用) |
| `[Ctrl]+[b]` / PageUp | 向上翻一页 (常用) |
| `[Ctrl]+[d]` | 向下移动半页 |
| `[Ctrl]+[u]` | 向上移动半页 |
| **+** | 光标移动到非空格符的下一行 |
| **-** | 光标移动到非空格符的上一行 |
| `n<space>` | 光标向右移动 n 个字符（n 为数字） |
| **0** / Home | 移动到当前行最前面 (常用) |
| **$** / End | 移动到当前行最后面 (常用) |
| **H** | 光标移动到屏幕最上方第一行 |
| **M** | 光标移动到屏幕中间那一行 |
| **L** | 光标移动到屏幕最下方那一行 |
| **G** | 移动到文件最后一行 (常用) |
| **nG** | 移动到文件第 n 行 (可配合 :set nu) |
| **gg** | 移动到文件第一行，相当于 1G (常用) |
| `n<Enter>` | 光标向下移动 n 行 (常用) |

### 默会知识 · 移动技巧

- **hjkl 肌肉记忆**：右手放在键盘上刚好排列在一起，养成不用看键盘也能摸到。别依赖方向键。这是vim最重要的默会知识——需要反复练习形成肌肉记忆。
- **数字前缀**：数字代表"次数"。`30j` = 向下移动 30 行，`20<space>` = 向右移动 20 个字符。数字永远在动作前面。
- **高频组合**：`0` 回行首，`$` 回行尾，`G` 到页尾，`gg` 到页头 — 这四个组合一定要形成条件反射。

---

## 第一部分：一般模式 - 搜索替换

### 明确知识：按键对照表

| 按键 | 功能说明 |
|------|----------|
| `/word` | 向下寻找名为 word 的字符串 (常用) |
| `?word` | 向上寻找名为 word 的字符串 |
| **n** | 重复前一个搜寻动作 |
| **N** | 反向重复前一个搜寻动作 |
| `:n1,n2s/word1/word2/g` | 在 n1~n2 行之间替换 word1 为 word2 |
| `:%s/word1/word2/g` 或 `:1,$s/word1/word2/g` | 全文替换 word1 为 word2 (常用) |
| `:%s/word1/word2/gc` 或 `:1,$s/word1/word2/gc` | 全文替换并要求确认每一处 (常用) |

### 默会知识 · 搜索技巧

- `/` 和 `n`/`N` 配合，可以快速遍历所有匹配项，比手动查找快得多。
- `c` 后缀的确认模式看似麻烦，但防止误替换，特别是全局替换前最好加上。
- 善用正则表达式可以做更复杂的替换，这需要慢慢积累经验。

---

## 第一部分：一般模式 - 删除、复制与粘贴

### 明确知识：按键对照表

| 按键 | 功能说明 |
|------|----------|
| **x** | 向后删除一个字符（相当于 Del）|
| **X** | 向前删除一个字符（相当于 Backspace）|
| **nx** | 连续向后删除 n 个字符 |
| **dd** | 剪切当前整行 (常用)，用 p/P 可粘贴 |
| **ndd** | 剪切向下 n 行 (常用)，`20dd` = 剪切 20 行 |
| **d1G** | 删除光标到第一行的所有数据 |
| **dG** | 删除光标到最后一行的所有数据 |
| **d$** | 删除光标到行末 |
| **d0** | 删除光标到行首 |
| **yy** | 复制当前行 (常用) |
| **nyy** | 复制向下 n 行 (常用) |
| **y1G** | 复制当前行到第一行 |
| **yG** | 复制当前行到最后一行 |
| **y0** | 复制光标到行首 |
| **y$** | 复制光标到行尾 |
| **p** | 将已复制的数据贴在光标**下一行** |
| **P** | 将已复制的数据贴在光标**上一行** |
| **J** | 将当前行与下一行合并为一行 |
| **c** | 重复删除多个数据，例如 `10cj` 向下删除 10 行 |
| **u** | 复原前一个动作 (常用) |
| `[Ctrl]+r` | 重做上一个动作 (常用) |
| **.** | 重复前一个动作 (常用) |

### 默会知识 · 编辑技巧

- **养成 `u` 和 `Ctrl+r` 会让你非常安心 — 错了可以复原，这是vim编辑的安全感来源。
- **dd p = 移动行，yy p = 复制行。这两个组合一定要练到不用想就会用。
- **`.` 点号非常强大**：重复删除、重复粘贴、重复替换，善用可以省去重复劳动。这是很多老手都没完全用好的默会技巧。
- 数字真的很重要！`5dd` 一次删 5 行比连按 5 次 dd，这就是vim思维。

---

## 第二部分：一般模式切换到编辑模式

### 明确知识：进入编辑模式对照表

| 按键 | 功能说明 |
|------|----------|
| **i** | 从**光标当前位置**开始输入 (常用) |
| **I** | 从当前行第一个非空格符开始输入 (常用) |
| **a** | 从光标**下一个字符**开始输入 (常用) |
| **A** | 从光标所在行**最后一个字符**开始输入 |
| **o** | 在光标**下一行**插入新行并进入输入模式 (常用) |
| **O** | 在光标**上一行**插入新行并进入输入模式 |
| **r** | 只取代光标所在的那一个字符一次 |
| **R** | 一直取代光标所在文字，直到按下 ESC 为止 (常用) |
| **Esc** | 退出编辑模式，回到一般模式 (常用) |

### 默会知识 · 切换技巧

- **i 是_insert，a 是_append**，记住这个词源，就永远不会忘。
- **快速切换是一种肌肉记忆**：编辑完就按 ESC，不用一直按着不放，这是vim和其他编辑器最大的区别。老手可以做到"编辑 → 移动 → 编辑 → 移动，全程手不离开主键盘区。
- **o 非常常用**：在当前行下方新开一行，比先回车再输入快得多。形成习惯后效率提升非常明显。
- **左下角看状态**：一定要看左下角有没有 `--INSERT--` 或 `--REPLACE--` 确认当前模式。

---

## 第三部分：指令行模式 - 储存、离开等指令

### 明确知识：指令对照表

| 指令 | 功能说明 |
|------|----------|
| `:w` | 将编辑的数据写入硬盘 (常用) |
| `:w!` | 强制写入只读属性的文件 |
| `:q` | 离开 vi (常用) |
| `:q!` | 不保存强制离开，放弃修改 |
| `:wq` | 保存后离开 (常用) |
| `:wq!` | 强制保存后离开 |
| **ZZ** | 保存修改后退出（大写Z，效果同 wq |
| **ZQ** | 不保存强制退出（效果同 q! |
| `:w [filename]` | 另存为新文件 |
| `:r [filename]` | 读取 filename 的内容加到当前行后 |
| `:n1,n2 w [filename]` | 将 n1 到 n2 的内容另存为文件 |
| `:! command` | 暂时离开 vi 执行外部命令 |
| `:set nu` | 显示行号 |
| `:set nonu` | 取消行号显示 |

### 默会知识 · 工作流

- **! 在 vi 中常常代表「强制」**：`q!` 强制退出，`wq!` 强制写。记住这个潜规则。
- **ZZ 比 :wq 更快**，手不用移动就能退出，养成习惯后很爽。
- **`:!command` 这是vim和 shell 打通的关键。比如 `:!ls` 看目录，`%!sort` 排序当前文件 — 这是把vim当编辑器和shell配合的强大能力。
- **数字的意义**：整个vim设计一致，数字永远代表「次数」或「第几个」，理解这个规律，所有命令都符合这个默会规则。

---

## 默会知识总结

### 波兰尼的默会知识在 Vim 中的体现

1. **我们知道的比我们能说出的多**：
   - 你能把所有按键都背下来，但真正的vim功力在肌肉记忆里
   - 高手的手指会自动做出反应，不需要思考"下一步按什么键

2. **做中学**：
   - 默会知识只能通过实践获得，看十遍不如自己练一遍
   - 逼着自己不用鼠标、不用方向键，一周就能入门

3. **技能迁移**：
   - 数字前缀、hjkl移动、模式切换——这些都是"只可意会不可言传"的技巧
   - 慢慢练，慢慢会

> *"我们知道的比我们能说出的要多得多。" -- 迈克尔·波兰尼"*
